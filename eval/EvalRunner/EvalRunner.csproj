<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>Fde.Eval</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- AGT core: used only by the `promptdefense` command (system-prompt
         hardening grade). M2/M3 are BCL-only (HttpClient + System.Text.Json),
         same no-SDK rule as src/Leaderboard/LangfuseClient.cs. -->
    <PackageReference Include="Microsoft.AgentGovernance" Version="3.3.0" />
    <PackageReference Include="DotNetEnv" Version="3.1.1" />
  </ItemGroup>

</Project>